name: gazelle_cors
description: An easy to use plugin to handle CORS with Gazelle framework.
version: 0.1.0
repository: https://github.com/intales/gazelle/packages/gazelle_cors
publish_to: none

environment:
  sdk: ^3.3.2

dev_dependencies:
  http: ^1.2.1
  lints: ^3.0.0
  test: ^1.24.0
dependencies:
  gazelle_core: ^0.1.6
