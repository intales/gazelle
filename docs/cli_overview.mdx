# CLI Overview

<Warning>The CLI is currently under heavy development, please report any bug or issue with it.</Warning>

The Gazelle CLI is a tool to help you with development of Gazelle applications.
It currently supports project bootstrapping and dockerization to help you
ship your app to any cloud provider that supports containers.

## Installation
To install the CLI just run:
```shell
dart pub global activate gazelle_cli
```
